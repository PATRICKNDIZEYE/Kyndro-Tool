# Redocly lint configuration for the Kyndro contract (toolchain: ADR-007).
# Usage: npx --yes @redocly/cli@1.34.3 lint --config contract/redocly.yaml contract/openapi.yaml
extends:
  - recommended
rules:
  # The mock server entry legitimately points at localhost.
  no-server-example.com: off
