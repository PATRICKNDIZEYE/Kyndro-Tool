# Digest — 2026-07-02 (W0 bootstrap)

## Merged since yesterday
Nothing — this is day zero; the repo was initialized today.

## PRs awaiting human gate
- **"W0: bootstrap"** (`bootstrap/w0` → `main`) — gate **G-WAVE-0**. QA-CRITIC is not yet
  spawned (it comes online with W1), so this one PR reaches you unfiltered. Your approval
  opens W1; ORCH does nothing further until you say **"open W1"**.

## Created today (W0 seed checklist, PLAN.md §9)
- `PLAN.md` — canonical copy of the agentic build plan (original kept, see D4).
- `kyndro-sprint-plan.md` — canonical copy of the sprint plan (PLAN.md §7 references this filename).
- `CLAUDE.md` — 10 lines: §3/§4 pointers + the seven §6 NEVERs verbatim.
- 17 territory directories scaffolded with `.gitkeep` (contract, fixtures, tasks, tasks/blocked, reviews, reviews/sec, digests, docs/adr, demos, engine, platform, infra, app, app/design-system, site, docs-site, cli).
- `docs/adr/ADR-000-template.md`.
- Six W1 packets in `/tasks/` per the §4.1 schema, enriched from Sprint 1: `W1-PLAT-1` (**todo** — sequential-first contract packet), `W1-PLAT-2`, `W1-CORE-1`, `W1-SYS-1`, `W1-SYS-2`, `W1-APP-1` (all **blocked**, `depends_on: [W1-PLAT-1]`, per §1 P4).
- `demos/w1.md` — click-by-click G-WAVE-1 exit script.
- `HUMAN-SETUP.md` — your to-do list: branch protection, GitHub App/OAuth registration, secret NAMES, ready-to-paste §4.3 hooks block, agent-teams flag.

## Blocked tasks + proposed re-scopes
Five W1 packets are `blocked` **by design** (contract freezes before fan-out, §1 P4) — no
re-scope needed. W0-9 was briefly blocked by a harness shell outage (P7 applied); resolved
same day when H-PO initialized and pushed the repo manually — blocker report removed in the
W0 closure PR. Zero packets blocked by failure.

## Decisions needed (max 5, each with a recommendation)

**D1 — GitHub repo home. ✅ RESOLVED by H-PO 2026-07-02:** you created
https://github.com/PATRICKNDIZEYE/Kyndro-Tool and pushed `main` (root commit `549f3b7`)
directly — a one-time human act before branch protection existed. Still outstanding and
human-only: turn branch protection ON (HUMAN-SETUP.md §2 — a W1 entry criterion), verify the
repo is private, and fix your git identity (commits currently authored as
"SSH Azul <sshazul@192.168.1.76>"; command in HUMAN-SETUP.md §1).

**D2 — Dependency-gate granularity.** PLAN.md §5/§6 require ADR + G-DEP for every new
dependency, but the sprint plan already prescribes tools (Prism, MSW, tree-sitter, Storybook,
Hypothesis...). Taken literally, W1 would generate ~6 G-DEP interrupts. *Recommended:* at each
wave open, agents batch their toolchain choices into ONE "wave baseline" ADR you approve once;
per-dependency G-DEP applies only to additions after that. Packets are written to file ADRs
either way, so no packet changes needed whichever you choose.

**D3 — Package manager / monorepo tool.** PLAN.md §4.1's example uses `pnpm`; the sprint plan
says `npm run dev`. *Recommended:* pnpm workspaces (matches PLAN.md, the newer document), and
read the sprint plan's `npm run dev` as generic "the dev command". W1-PLAT-2 encodes the
choice in its monorepo ADR.

**D4 — Source-file cleanup.** Your instructions named `kyndro-agentic-build-plan.md` /
`kyndro-sprint-plan.md`, but the directory contains `General.MD` (= the agentic build plan),
`Guide.MDes` (= the sprint plan), and `General.MDes` (byte-identical duplicate of General.MD).
ORCH copied content to canonical names and committed those; the three originals are untouched,
untracked, and gitignored. *Recommended:* delete `General.MDes` (pure duplicate) and keep the
other two as local-only originals — deletion is yours to do, agents don't delete your files.

**D5 — `blocked` status vs the §4.1 lifecycle.** You mandated `status: blocked` for the five
parallel packets, but §4.1's lifecycle enum reads `todo → claimed → in_review → human_gate →
done` (its `depends_on` comment does say "blocked tasks are not spawned"). *Recommended:*
amend §4.1 in PLAN.md to `blocked → todo → claimed → ...` in a two-line follow-up PR after W0
merges, so the schema and the packets on disk agree. Not amended yet — PLAN.md changes are
yours to approve.

## Ambiguous / conflicting between the two documents (flagged, not silently resolved)
1. **pnpm vs npm** — see D2/D3.
2. **Prescribed tools vs G-DEP** — sprint plan names vendors; PLAN.md gates every dependency. See D2.
3. **Sprint 1 "GitHub OAuth app registration + token model design"** has no W1 packet: the wave
   plan defers the GitHub App and auth to W2 (W2-PLAT-1/-3), yet contract v1 still includes auth
   endpoints. W0 treatment: registration = human task (HUMAN-SETUP.md §4); token-model design
   lands with W2 packets; W1-PLAT-1 only freezes auth API shapes.
4. **Sprint 1 wireframes** (dashboard home, run detail, spec inbox, onboarding) map to no W1
   packet row. Options: fold into W1-SYS-1, or let each screen's first implementation hit its
   own G-UX gate in W2/W3 (the wave plan's apparent intent). Currently in no packet.
5. **"Waitlist capture live by end of sprint"** (Sprint 1) vs **G-PUB is human-only** (PLAN.md).
   W1-SYS-2 is written as ready-to-publish + files a G-PUB request; nothing self-deploys.
6. **W1 exit gate says "mock dashboard renders fixtures"** but the W1-APP-1 table row only says
   "mock server + MSW handlers". W1-APP-1 was expanded to include a minimal run-list/run-detail
   shell so the exit gate is achievable — flagged here since it enlarges the row's scope.

## Burn
- **W0 seed checklist:** 6/8 agent items done; awaiting this PR's merge. Human items outstanding: branch protection ON, hooks pasted into `.claude/settings.json` (both in HUMAN-SETUP.md — W1 entry criteria per PLAN.md §7).
- **W1 packets:** 0/6 done · 1 todo (W1-PLAT-1) · 5 blocked-by-design.
- **Health:** blocked-by-failure count 0 · human-review latency n/a (first PR) · dogfood FALSIFIED catches n/a (no engine yet).
